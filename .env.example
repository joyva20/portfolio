# Public (safe to expose in browser)
# EmailJS (used by Contact form on GitHub Pages)
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=

# Optional: if you still deploy to Vercel/Netlify with server APIs later,
# you can keep those secrets in the platform's env settings instead.
